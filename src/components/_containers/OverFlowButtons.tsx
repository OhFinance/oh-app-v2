import styled from 'styled-components';
import CenteredRow from './CenteredRow';

export const OverFlowButtons = styled(CenteredRow)({
  position: 'absolute',
  bottom: '-35px',
  padding: '0 50px',
  gap: '7px',
});

export default OverFlowButtons;
